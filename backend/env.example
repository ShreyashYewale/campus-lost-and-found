# Local development (no PostgreSQL required)
DATABASE_URL=file:./keystone.db
SESSION_SECRET=dev-only-session-secret-change-in-production-min-32-chars

# Production / team PostgreSQL (optional)
# DATABASE_URL=postgres://postgres:yourpassword@localhost:5432/lostfound
